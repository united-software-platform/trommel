# Покрытие нормы

Состояние каждого правила действующей нормы: что Trommel гарантирует, что остаётся непроверенным и почему. Документ порождается прогоном и вручную не правится.

---

## Навигация

- [Сводка](#сводка)
- [Правила состава](#правила-состава)
- [Версионирование](#версионирование)

---

## Сводка

| Показатель | Значение |
|------------|----------|
| Правил в норме | 124 |
| В составе прогона | 124 |
| Проверено | 10 |
| Гарантировано | 8 |
| Нарушено | 2 |
| Проверка не состоялась | 0 |
| Неприменимо | 0 |
| Не проверяется | 114 |
| Каталог нормы | `rules` |
| Отпечаток нормы | `6cdea57f3842` |
| Контракт | `full` |
| Вне обхода | `openspec` |

---

## Правила состава

| Код | Правило | Состояние | Проверка |
|-----|---------|-----------|----------|
| `CLAR-001` | Зависимости направлены строго внутрь: инфраструктура → приложение → домен | не проверяется | ложится на граф зависимостей внешнего анализатора кода |
| `CLAR-002` | Домен содержит только бизнес-правила: сущности, значения, доменные события, контракты репозиториев, доменные сервисы | не проверяется | предмет — исходный код; разбор фактов внешним анализатором не подключён |
| `CLAR-003` | Операции сохранения в контракте репозитория принимают доменную модель целиком — не набор скаляров, не транспортную структуру | не проверяется | предмет — исходный код; разбор фактов внешним анализатором не подключён |
| `CLAR-004` | Домен не зависит от фреймворка, библиотеки доступа к данным, контейнера внедрения зависимостей и транспорта | не проверяется | предмет — исходный код; разбор фактов внешним анализатором не подключён |
| `CLAR-006` | Домен не объявляет контракт, реализация которого обязана обратиться к инфраструктуре | не проверяется | предмет — исходный код; разбор фактов внешним анализатором не подключён |
| `CLAR-007` | Каждая бизнес-операция — отдельный сценарий использования; один сценарий — одно действие | не проверяется | машинного критерия не существует: правило задаёт намерение, а не границу множества нарушений |
| `CLAR-008` | У сценария использования ровно одна публичная операция: принимает входной контракт, возвращает выходной либо ничего | не проверяется | предмет — исходный код; разбор фактов внешним анализатором не подключён |
| `CLAR-009` | Входной и выходной контракты неизменяемы и логики не содержат | не проверяется | предмет — исходный код; разбор фактов внешним анализатором не подключён |
| `CLAR-010` | Сценарий использования зависит только от контрактов домена, не от их реализаций | не проверяется | предмет — исходный код; разбор фактов внешним анализатором не подключён |
| `CLAR-011` | Сценарий использования вместе со своими входным и выходным контрактами образует отдельную единицу группировки | не проверяется | предмет — исходный код; разбор фактов внешним анализатором не подключён |
| `CLAR-012` | Слой приложения не обращается к хранилищу, транспорту и внешним службам напрямую — только через контракты | не проверяется | предмет — исходный код; разбор фактов внешним анализатором не подключён |
| `CLAR-013` | Слой приложения не содержит бизнес-логики: инварианты и расчёты принадлежат домену | не проверяется | «бизнес-логика» не имеет машинного критерия; правило задаёт намерение |
| `CLAR-014` | Реализация репозитория сохраняет ровно один агрегат | не проверяется | предмет — исходный код; разбор фактов внешним анализатором не подключён |
| `CLAR-015` | Слой инфраструктуры не содержит бизнес-логики | не проверяется | «бизнес-логика» не имеет машинного критерия; правило задаёт намерение |
| `CLAR-016` | Обработчик входящего запроса не зависит от доменных объектов напрямую — только через слой приложения | не проверяется | предмет — исходный код; разбор фактов внешним анализатором не подключён |
| `CLAR-017` | SOLID/SRP — у компонента ровно одна причина меняться | не проверяется | машинного критерия не существует: правило задаёт намерение, а не границу множества нарушений |
| `CLAR-018` | SOLID/OCP — расширение новым компонентом, а не правкой существующего | не проверяется | машинного критерия не существует: правило задаёт намерение, а не границу множества нарушений |
| `CLAR-019` | SOLID/LSP — реализация полностью соблюдает контракт | не проверяется | машинного критерия не существует: правило задаёт намерение, а не границу множества нарушений |
| `CLAR-020` | SOLID/ISP — контракты узкие и сфокусированные | не проверяется | машинного критерия не существует: правило задаёт намерение, а не границу множества нарушений |
| `CLAR-021` | SOLID/DIP — внешние слои зависят от абстракций, объявленных в домене | не проверяется | предмет — исходный код; разбор фактов внешним анализатором не подключён |
| `CLAR-022` | Код группируется по доменным модулям, а не по техническим слоям | не проверяется | предмет — исходный код; разбор фактов внешним анализатором не подключён |
| `CLAR-023` | Межмодульные зависимости — только через контракты домена | не проверяется | предмет — исходный код; разбор фактов внешним анализатором не подключён |
| `CQRS-001` | Контракт записи объявляется в домене и содержит только изменяющие операции | не проверяется | предмет — исходный код; разбор фактов внешним анализатором не подключён |
| `CQRS-002` | Контракт записи не содержит операций чтения | не проверяется | предмет — исходный код; разбор фактов внешним анализатором не подключён |
| `CQRS-003` | Контракт чтения объявляется в слое приложения и возвращает только модели чтения | не проверяется | предмет — исходный код; разбор фактов внешним анализатором не подключён |
| `CQRS-004` | Контракт чтения не содержит изменяющих операций | не проверяется | предмет — исходный код; разбор фактов внешним анализатором не подключён |
| `CQRS-005` | Сценарий-запрос зависит только от контракта чтения | не проверяется | предмет — исходный код; разбор фактов внешним анализатором не подключён |
| `CQRS-006` | Реализации контрактов чтения и записи — отдельные компоненты слоя инфраструктуры | не проверяется | предмет — исходный код; разбор фактов внешним анализатором не подключён |
| `DDD-001` | Единый язык (Ubiquitous Language) — все термины в коде, документах и разговорах совпадают | не проверяется | машинного критерия не существует: правило задаёт намерение, а не границу множества нарушений |
| `DDD-002` | Запрет технических синонимов для доменных понятий внутри одного контекста | не проверяется | машинного критерия не существует: правило задаёт намерение, а не границу множества нарушений |
| `DDD-003` | Каждый Bounded Context имеет явную границу и единую согласованную модель внутри | не проверяется | машинного критерия не существует: правило задаёт намерение, а не границу множества нарушений |
| `DDD-004` | Одно доменное понятие может иметь разный смысл в разных контекстах — это нормально | не проверяется | машинного критерия не существует: правило задаёт намерение, а не границу множества нарушений |
| `DDD-005` | Взаимодействие между контекстами — только через явный контракт (ACL, события, API) | не проверяется | предмет — исходный код; разбор фактов внешним анализатором не подключён |
| `DDD-006` | Агрегат — единица транзакционной согласованности; инвариант соблюдается внутри агрегата | не проверяется | машинного критерия не существует: правило задаёт намерение, а не границу множества нарушений |
| `DDD-007` | Корень агрегата (Aggregate Root) — единственная точка входа; прямой доступ к внутренним объектам запрещён | не проверяется | предмет — исходный код; разбор фактов внешним анализатором не подключён |
| `DDD-008` | Ссылки между агрегатами — только по идентификатору, не по объекту | не проверяется | требует типов полей: есть ли они в модели фактов анализатора — предстоит проверить |
| `DDD-009` | Один агрегат изменяется в одной транзакции; изменение нескольких агрегатов — через Domain Events | не проверяется | предмет — исходный код; разбор фактов внешним анализатором не подключён |
| `DDD-010` | Сущность (Entity) — идентифицируется по id, не по значениям свойств | не проверяется | предмет — исходный код; разбор фактов внешним анализатором не подключён |
| `DDD-011` | Жизненный цикл сущности отслеживается через Repository | не проверяется | предмет — исходный код; разбор фактов внешним анализатором не подключён |
| `DDD-012` | Value Object — идентифицируется по совокупности значений, не имеет id | не проверяется | предмет — исходный код; разбор фактов внешним анализатором не подключён |
| `DDD-013` | Value Object — всегда иммутабельный; изменение создаёт новый объект | не проверяется | предмет — исходный код; разбор фактов внешним анализатором не подключён |
| `DDD-014` | Валидация инвариантов Value Object — в конструкторе или фабрике; невалидный объект не создаётся | не проверяется | предмет — исходный код; разбор фактов внешним анализатором не подключён |
| `DDD-015` | Domain Event — факт, произошедший в домене; неизменяем после создания | не проверяется | предмет — исходный код; разбор фактов внешним анализатором не подключён |
| `DDD-016` | Название Domain Event — прошедшее время: `OrderPlaced`, `PaymentConfirmed` | не проверяется | предмет — исходный код; разбор фактов внешним анализатором не подключён |
| `DDD-017` | Domain Event содержит только данные, необходимые подписчику; никакой логики | не проверяется | предмет — исходный код; разбор фактов внешним анализатором не подключён |
| `DDD-018` | Domain Service применяется только если операция не принадлежит ни одной сущности/VO | не проверяется | машинного критерия не существует: правило задаёт намерение, а не границу множества нарушений |
| `DDD-019` | Domain Service не хранит состояние (stateless) | не проверяется | предмет — исходный код; разбор фактов внешним анализатором не подключён |
| `DDD-020` | Repository — интерфейс в Domain, реализация в Infrastructure | не проверяется | предмет — исходный код; разбор фактов внешним анализатором не подключён |
| `DDD-021` | Repository работает с агрегатами целиком; загрузка частей агрегата запрещена | не проверяется | предмет — исходный код; разбор фактов внешним анализатором не подключён |
| `DDD-022` | Запрет бизнес-логики в Repository; только операции хранения и выборки | не проверяется | машинного критерия не существует: правило задаёт намерение, а не границу множества нарушений |
| `DDD-023` | Factory инкапсулирует сложную логику создания агрегатов и сущностей | не проверяется | машинного критерия не существует: правило задаёт намерение, а не границу множества нарушений |
| `DDD-024` | Объект не создаёт сам себя — только через Factory или конструктор с явными аргументами | не проверяется | предмет — исходный код; разбор фактов внешним анализатором не подключён |
| `DDD-025` | ACL (Anti-Corruption Layer) — обязателен при интеграции с внешней системой или чужим контекстом | не проверяется | машинного критерия не существует: правило задаёт намерение, а не границу множества нарушений |
| `DDD-026` | ACL переводит чужую модель в термины своего контекста; внешняя модель не проникает в домен | не проверяется | предмет — исходный код; разбор фактов внешним анализатором не подключён |
| `DOCK-001` | Имена сервисов — с префиксом проекта; префикс образуется от имени проекта | не проверяется | проверяемо действующим предметом прогона; реализация не написана |
| `DOCK-002` | `container_name` совпадает с именем сервиса | не проверяется | проверяемо действующим предметом прогона; реализация не написана |
| `DOCK-003` | Обязательно объявлять сеть в `networks` и подключать к ней все сервисы | не проверяется | проверяемо действующим предметом прогона; реализация не написана |
| `DOCK-004` | Запрет атрибута `env_file` | не проверяется | проверяемо действующим предметом прогона; реализация не написана |
| `DOCK-005` | `working_dir` указывается при необходимости — прежде всего для контейнера приложения | не проверяется | машинного критерия не существует: правило задаёт намерение, а не границу множества нарушений |
| `DOCK-006` | `healthcheck` добавляется при необходимости — прежде всего там, где от готовности сервиса зависят другие | не проверяется | машинного критерия не существует: правило задаёт намерение, а не границу множества нарушений |
| `DOCK-007` | Политика перезапуска — `restart: unless-stopped` для всех долгоживущих сервисов; исключение — сервисы инициализации состояния (DOCK-014) | не проверяется | проверяемо действующим предметом прогона; реализация не написана |
| `DOCK-008` | Порты по умолчанию не назначаются; публикуемый порт выбирается из диапазона, закреплённого за проектом; по возможности проверяется, свободен ли порт | не проверяется | машинного критерия не существует: правило задаёт намерение, а не границу множества нарушений |
| `DOCK-009` | Все параметры `environment` берутся из `.env` (через интерполяцию `${VAR}`) | не проверяется | проверяемо действующим предметом прогона; реализация не написана |
| `DOCK-010` | Рядом с `.env` создаётся `.env.example` — шаблон переменных окружения | не проверяется | проверяемо действующим предметом прогона; реализация не написана |
| `DOCK-011` | Файл конфигурации Compose именуется `docker-compose.yaml` | не проверяется | проверяемо действующим предметом прогона; реализация не написана |
| `DOCK-012` | Контейнер приложения запускается под непривилегированным пользователем, а не под `root` | не проверяется | проверяемо действующим предметом прогона; реализация не написана |
| `DOCK-013` | Рабочий каталог и все внутренние файлы контейнера приложения принадлежат этому пользователю | не проверяется | проверяемо действующим предметом прогона; реализация не написана |
| `DOCK-014` | Состояние в томах, необходимое сервису для работы (модели, словари, расширения, схемы), наполняется одноразовым сервисом инициализации `{сервис}-init` с `restart: "no"`; ручное наполнение тома запрещено | не проверяется | машинного критерия не существует: правило задаёт намерение, а не границу множества нарушений |
| `DOCK-015` | Потребители инициализированного состояния ждут завершения init-сервиса через `depends_on` с `condition: service_completed_successfully`; сам init-сервис ждёт готовности целевого сервиса по `healthcheck` | не проверяется | проверяемо действующим предметом прогона; реализация не написана |
| `DOC-001` | Документация, комментарии, ответы — на русском языке | не проверяется | машинного критерия не существует: правило задаёт намерение, а не границу множества нарушений |
| `DOC-002` | Имена переменных, классов, методов — на английском | не проверяется | предмет — исходный код; разбор фактов внешним анализатором не подключён |
| `DOC-003` | Термины без устоявшегося перевода — на английском | не проверяется | машинного критерия не существует: правило задаёт намерение, а не границу множества нарушений |
| `DOC-004` | Markdown — CommonMark/GitHub-style, совместимый с GitLab и Obsidian; положение о формате, исполнимые запреты — DOC-005..DOC-008 | не проверяется | сама норма помечает правило непроверяемым: множество нестандартных расширений не перечислимо |
| `DOC-005` | Запрет HTML целиком: любая конструкция, относимая CommonMark к HTML, вне кода | гарантировано | Конструкции, относимые CommonMark к HTML, вне вставок и блоков кода; автоссылка нарушением не считается |
| `DOC-006` | Запрет Wikilinks `[[Note]]` | гарантировано | Ссылки формата Wikilink в двойных квадратных скобках, вне вставок и блоков кода |
| `DOC-007` | Запрет четырёхпробельного отступа вместо fenced code blocks | гарантировано | Блок с отступом в четыре пробела и более после пустой строки вне списка |
| `DOC-008` | Запрет подчёркивания заголовков `====` / `----` | гарантировано | Строка из знаков = или - под непустым текстом; тематический разделитель и разделитель таблицы исключены |
| `DOC-009` | Строго структурированный текст — логичные разделы, короткие абзацы | не проверяется | машинного критерия не существует: правило задаёт намерение, а не границу множества нарушений |
| `DOC-010` | Один раздел — один вопрос | не проверяется | машинного критерия не существует: правило задаёт намерение, а не границу множества нарушений |
| `DOC-011` | Сложные темы — в отдельные файлы; в текущем — резюме и ссылка | не проверяется | машинного критерия не существует: правило задаёт намерение, а не границу множества нарушений |
| `DOC-012` | Иерархия заголовков `#` → `##` → `###` → `####`, без пропусков | гарантировано | Переход с уровня заголовка на уровень, отстоящий более чем на единицу |
| `DOC-013` | Маркированные списки — `-`, нумерованные — `1.`, вложенность ≤ 2 уровней | не проверяется | проверяемо действующим предметом прогона; реализация не написана |
| `DOC-014` | Каждый файл начинается с заголовка, назначения (1–3 строки) и навигации | нарушено | Порядок трёх открывающих элементов документа: заголовок первого уровня, назначение, секция навигации |
| `DOC-015` | Только относительные ссылки: `[текст](./path/file.md#anchor)`; исключение — `README.md` там, где цель ссылки действительно внешняя | гарантировано | Адреса со схемой, протоколо-независимые и отсчитываемые от корня; README.md исключён самим правилом |
| `DOC-016` | Только fenced code blocks с указанием языка | гарантировано | Открывающий забор огороженного блока кода без слова языка |
| `DOC-017` | Таблицы — стандартный Markdown ` | не проверяется | проверяемо действующим предметом прогона; реализация не написана |
| `DOC-018` | Диаграммы — Mermaid; для сложных C4 — PlantUML в отдельных `.puml` файлах | не проверяется | проверяемо действующим предметом прогона; реализация не написана |
| `DOC-019` | Callout-блоки — blockquote с маркером: `> **Важно:**` / `> **Внимание:**` | не проверяется | проверяемо действующим предметом прогона; реализация не написана |
| `DOC-020` | Явно фиксировать ограничения, допущения, риски | не проверяется | машинного критерия не существует: правило задаёт намерение, а не границу множества нарушений |
| `DOC-022` | Версионирование в конце каждого md-файла — таблица с историей изменений, источником изменения, агентом и моделью; исключение — `README.md` | нарушено | Наличие таблицы версий документа; README.md исключён самим правилом |
| `DOC-023` | При переносе контента — в источнике оставить ссылку на новое расположение | не проверяется | машинного критерия не существует: правило задаёт намерение, а не границу множества нарушений |
| `DOC-024` | Строки таблицы версий идут по возрастанию версии: первая запись — сверху, актуальная версия — в последней строке | гарантировано | Возрастание номеров версий сверху вниз в таблице версий |
| `GIT-005` | Ветка должна отражать одну законченную задачу или один логически связанный поток изменений | не проверяется | машинного критерия не существует: правило задаёт намерение, а не границу множества нарушений |
| `GIT-006` | Имена долгоживущих веток `main`, `master`, `develop` используются только как защищённые базовые ветки | не проверяется | проверяемо действующим предметом прогона; реализация не написана |
| `GIT-009` | Сообщение коммита состоит из заголовка `{CODE}-NNN: описание задачи`, пустой строки и тела с перечнем выполненной работы | не проверяется | предмет уже поддержан — текст сообщения коммита; выражение заголовка задано самой нормой |
| `GIT-010` | Перед созданием коммита агент или разработчик уточняет код, номер и описание задачи, если они не были явно указаны | не проверяется | предмет — действие, а не артефакт: перехватывается хуком, а не прогоном |
| `GIT-012` | В сообщения коммитов и описания PR/MR не добавляются служебные подписи об авторстве или генерации любых LLM-агентов и инструментов (`🤖 Generated with …`, `Co-Authored-By: <LLM-агент> …`) | не проверяется | предмет уже поддержан — текст сообщения коммита; реализация не написана |
| `GIT-015` | Коммит и push выполняются только по явному запросу пользователя | не проверяется | предмет — действие, а не артефакт: перехватывается хуком, а не прогоном |
| `GIT-016` | Без явного запроса — подготовить изменения, показать состав коммита и предложенное сообщение, дождаться подтверждения | не проверяется | предмет — действие, а не артефакт: перехватывается хуком, а не прогоном |
| `GIT-017` | Запрещены `push --force`, `git reset --hard`, `git checkout .` и другие деструктивные операции без явного разрешения пользователя | не проверяется | предмет — действие, а не артефакт: перехватывается хуком, а не прогоном |
| `GIT-018` | Запрещено восстанавливать файлы через git (`git checkout HEAD -- …`, `git restore`, `git stash pop` и т.п.) без явного указания пользователя | не проверяется | предмет — действие, а не артефакт: перехватывается хуком, а не прогоном |
| `GIT-019` | Запрещено читать историю git (`git log`, `git show`, `git blame` и т.п.) и сообщения коммитов без явного указания пользователя | не проверяется | предмет — действие, а не артефакт: перехватывается хуком, а не прогоном |
| `GIT-020` | Гейт редактирования: перед изменением файла проверить наличие в нём незакоммиченных правок; при их наличии — сначала закоммитить их, затем вносить новые | не проверяется | предмет — действие, а не артефакт: перехватывается хуком, а не прогоном |
| `GIT-021` | Машинный релизный коммит — созданный релизным сценарием репозитория, изменяющий только файл версии и озаглавленный `Релиз vX.Y.Z` — освобождается от требований `GIT-009` и `GIT-010` к коду и номеру задачи | не проверяется | проверяется вместе с GIT-009; реализация не написана |
| `MAKE-001` | Объём секции цели не превышает 30 строк; норма относится к цели, а не к файлу целиком | не проверяется | проверяется вне Trommel: tools/make-lint/check.sh из поставки набора |
| `MAKE-002` | Превышение устраняется дроблением цели на под-цели или выносом логики рецепта в скрипт; повышение предела проверки способом устранения не является | не проверяется | машинного критерия не существует: правило задаёт намерение, а не границу множества нарушений |
| `MAKE-003` | Под-цель после дробления идемпотентна и вызывается отдельно; исходная цель остаётся точкой входа и последовательно вызывает под-цели | не проверяется | машинного критерия не существует: правило задаёт намерение, а не границу множества нарушений |
| `MAKE-004` | Перед отправкой изменений, затрагивающих `Makefile`, обязателен локальный прогон `tools/make-lint/check.sh` | не проверяется | предмет — действие, а не артефакт: перехватывается хуком, а не прогоном |
| `MAKE-005` | Блокирующий предел в пайплайне репозитория kit'а работает как храповик: не увеличивается, снижается по мере устранения превышений, целевое значение — 30 | не проверяется | проверяемо действующим предметом прогона; реализация не написана |
| `READ-001` | Перед КАЖДЫМ вызовом `Read`/`Grep`/`Glob` пройти гейт: цель чтения (файл или каталог) должна быть указана явно — в запросе пользователя, правилах проекта или тексте исполняемого сценария команды либо сабрутины | не проверяется | предмет — действие, а не артефакт: перехватывается хуком, а не прогоном |
| `READ-002` | Если путь не указан явно — СТОП: инструмент не вызывается, у пользователя запрашивается явное разрешение с обоснованием (какой документ, зачем, что именно требуется) | не проверяется | предмет — действие, а не артефакт: перехватывается хуком, а не прогоном |
| `READ-003` | «Похоже, пригодится для задачи» не является прохождением гейта; критерий — явное указание пути, а не оценка полезности | не проверяется | предмет — действие, а не артефакт: перехватывается хуком, а не прогоном |
| `READ-004` | Запрет чтения содержимого каталогов (включая `Read`, `Grep`, `Glob`): `openspec/changes/archive/` | не проверяется | предмет — действие, а не артефакт: перехватывается хуком, а не прогоном |
| `READ-005` | Сценарий команды или сабрутина даёт право читать только те пути, которые называет поимённо | не проверяется | предмет — действие, а не артефакт: перехватывается хуком, а не прогоном |
| `READ-006` | Ссылка на артефакт как на источник методики не даёт права читать его выходные файлы и отчёты | не проверяется | предмет — действие, а не артефакт: перехватывается хуком, а не прогоном |
| `TERM-001` | Понятие и его аббревиатура определяются один раз — в документе, который вводит понятие; остальные артефакты его потребляют, а не переопределяют | не проверяется | машинного критерия не существует: правило задаёт намерение, а не границу множества нарушений |
| `TERM-002` | Каждое понятие имеет одно каноническое полное имя и одну каноническую аббревиатуру; синонимы и альтернативные сокращения запрещены | не проверяется | машинного критерия не существует: правило задаёт намерение, а не границу множества нарушений |
| `TERM-003` | Аббревиатуры записываются латиницей по английскому полному имени понятия; параллельные кириллические сокращения того же понятия запрещены | не проверяется | проверяемо действующим предметом прогона; реализация не написана |
| `TERM-004` | При первом упоминании в документе понятие раскрывается полностью в каноническом формате: `Русское имя (English Name, Аббревиатура)`; далее по тексту — только аббревиатура | не проверяется | машинного критерия не существует: правило задаёт намерение, а не границу множества нарушений |
| `TERM-005` | Внутри одного документа одно понятие именуется единообразно; смешение формы (полное имя / аббревиатура / сокращение) для одной и той же сущности недопустимо | не проверяется | машинного критерия не существует: правило задаёт намерение, а не границу множества нарушений |
| `TERM-006` | Аббревиатура вводится вместе с раскрытием понятия; сокращение «по месту», без раскрытия, запрещено | не проверяется | машинного критерия не существует: правило задаёт намерение, а не границу множества нарушений |
| `TERM-007` | Аббревиатура не переопределяется: один акроним обозначает ровно одно понятие во всех артефактах | не проверяется | машинного критерия не существует: правило задаёт намерение, а не границу множества нарушений |
| `TERM-008` | Идентификатор экземпляра сущности — `{ПРЕФИКС}-{NNN}`, где префикс совпадает с канонической аббревиатурой понятия | не проверяется | формат кода уже применяется при построении каталога, но отдельной проверкой не оформлен |
| `TERM-009` | При изменении канонического имени или аббревиатуры правка вносится в документ-источник понятия и синхронно во все артефакты, ссылающиеся на него | не проверяется | машинного критерия не существует: правило задаёт намерение, а не границу множества нарушений |

---

## Версионирование

| Версия | Дата | Задача | Агент | Модель | Описание изменений |
|--------|------|--------|-------|--------|--------------------|
| 1.0.0 | 2026-09-22 | Порождение сводки покрытия прогоном | Trommel | — | Формат документа: сводка, правила состава с состоянием и границей проверки, правила вне состава с основанием исключения. Данные документа обновляются каждым прогоном и в этой таблице не отражаются |
