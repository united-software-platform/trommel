## 1. Возможности анализатора зафиксированы

- [x] 1.1 Снять с релиза `v0.4.0` перечень команд по уровням `L1`..`L5`, формы вывода, ключи
      области и способ определения версии; проверка — черновой перечень покрывает все команды,
      названные `README.md` и `docs/ARCHITECTURE.md` анализатора
- [x] 1.2 Выполнить `tldr --version`, `tldr structure`, `tldr imports`, `tldr calls`, `tldr slice`
      на дереве самого Trommel и сохранить образцы вывода в каталог образцов изменения;
      проверка — для каждой команды получен разбираемый JSON, отличия от документации выписаны
- [x] 1.3 Установить, поддерживает ли анализатор исключение каталогов собственным ключом
      (открытый вопрос `design.md`); проверка — ответ зафиксирован в документе возможностей
      с указанием проверенного ключа или его отсутствия
- [x] 1.4 Написать документ `docs/tldr-code.md`: назначение, уровни и команды, формы вывода,
      порядок установки, лицензия, границы применимости и то, чего анализатор не даёт;
      проверка — документ соответствует [DOC-014](../../../rules/documentation-rules.md#структура-файла)
      и [DOC-022](../../../rules/documentation-rules.md#жизненный-цикл-документа), ссылка на него
      добавлена в `README.md`

## 2. Анализатор в образе

- [x] 2.1 Перевести стадию выполнения `Dockerfile` с `alpine:3` на минимальный образ с glibc,
      сохранив непривилегированного пользователя и владение рабочим каталогом; проверка —
      `make trommel-image` собирается, `make trommel-image-verify` печатает непривилегированного
      пользователя, владельца каталога и отказ записи в смонтированный проект
- [x] 2.2 Добавить в `Dockerfile` получение бинарника анализатора: версия аргументом сборки,
      проверка контрольной суммы актива, укладка в `PATH` образа; проверка —
      `docker run --rm --entrypoint tldr <образ> --version` печатает закреплённую версию,
      а подмена суммы валит сборку
- [x] 2.3 Завести переменные выпуска анализатора (`TLDR_VERSION`, `TLDR_ASSET`, `TLDR_SHA256`)
      в `Makefile` рядом с `GO_IMAGE` и добавить проверку анализатора в `trommel-image-verify`;
      проверка — `sh tools/make-lint/check.sh` завершается кодом `0` [MAKE-004].
      Отступление от исходной формулировки: переменные заведены в `Makefile`, а не
      в `.env.example`, — их потребляет сборка образа на хосте, а не сервисы compose,
      и `.env` внутри контейнера намеренно пуст

## 3. Модель фактов и разбор вывода

- [x] 3.1 Описать внутреннюю модель фактов Trommel по уровням `L1`, `L2` и графу зависимостей
      и границу среза (непросмотренное с причиной); проверка — модель выражена типами пакета
      подготовки и не содержит полей, повторяющих форму JSON анализатора
- [x] 3.2 Реализовать разбор вывода анализатора в модель фактов с отказом на неопознанной
      форме; проверка — тесты на образцах задачи 1.2 дают ожидаемые факты, а на усечённом
      и на постороннем JSON — ошибку разбора, а не пустой срез
- [x] 3.3 Реализовать перенос непросмотренного в границу среза: неподдержанный язык, ошибка
      разбора, оборванная связь; проверка — тест на дереве с файлом неподдержанного языка даёт
      непустую границу с указанной причиной
- [x] 3.4 Реализовать сужение среза по исключённым каталогам на стороне Trommel; проверка —
      тест: факт из исключённого каталога в срез не попадает независимо от того, получил ли
      анализатор ключ исключения
- [x] 3.5 Реализовать обнаружение усечённого вывода: признак `truncated` анализатора переводится
      в непросмотренное, а не в факт; проверка — тест на образце с `truncated: true` даёт
      непустую границу среза и отсутствие статуса `ok` у правила, читавшего этот уровень

## 4. Запуск анализатора и фаза подготовки

- [x] 4.1 Реализовать запуск анализатора подпроцессом: команда, путь проекта, `--format json`,
      каталог временных данных вне проверяемого проекта, перенос кода возврата и текста ошибки
      в отказ прогона; проверка — тест с подставным исполняемым файлом покрывает успех,
      ненулевой код возврата и отсутствие исполняемого файла
- [x] 4.2 Реализовать проверку версии анализатора до подготовки с отказом вне поддерживаемого
      диапазона; проверка — тест: версия вне диапазона даёт отказ с указанием найденной
      и ожидаемой версий
- [x] 4.3 Добавить предмет проверки «факты о коде» в `rules.Subject` и срез в `rules.Context`;
      проверка — `make trommel-test` проходит, существующие реализации правил не затронуты
- [x] 4.4 Встроить фазу подготовки в `harness.Run` между проверкой исполнимости намерения
      и исполнением правил; проверка — тест: при составе без правил кода анализатор
      не запускается, при составе с правилами кода — запускается однократно
- [x] 4.5 Расширить `checkSubjects`: несостоявшаяся подготовка даёт отказ в запуске, а не статус
      `ok`; проверка — тест: правило, требующее фактов, при неудачной подготовке не исполняется,
      а код возврата обозначает несостоявшийся прогон

## 5. Контракт намерения

- [x] 5.1 Добавить в контракт намерения объявление состава среза перечнем уровней с отказом
      на неизвестном уровне и на уровне, недоступном обходом дерева; проверка — тесты:
      неизвестный уровень даёт отказ с перечнем доступных, уровень `L5` — отказ с причиной
      недоступности в фазе подготовки, объявленный проектный уровень — собирается
- [x] 5.2 Реализовать отказ при наличии правил кода в составе и отсутствии объявленного среза,
      и сообщение о неиспользованном срезе в обратном случае; проверка — тесты на оба случая
- [x] 5.3 Дополнить `trommel.yaml` разделом состава среза, не меняя состав контрактов `docs`
      и `full`; проверка — `./bin/trommel --contract docs` даёт прежние состояния правил

## 6. Вердикт

- [x] 6.1 Добавить в вердикт имя и версию анализатора при прогоне, опиравшемся на факты о коде;
      проверка — тест: в формах `text`, `json` и `markdown` присутствуют обе версии
- [x] 6.2 Добавить в вердикт перечень собранных уровней рядом с объявленными и объём границы
      среза; проверка — тест: собранный и пустой уровень отличим от несобранного
- [x] 6.3 Довести отражение границы среза до статуса правила: правило, опиравшееся
      на непросмотренное содержимое, не получает `ok`; проверка — тест на образцовой реализации
      правила, читающего факты

## 7. Сквозная проверка и сводка

- [x] 7.1 Прогнать `make trommel-fmt`, `make trommel-test`, `make trommel-build`; проверка —
      все три цели завершаются успешно
- [x] 7.2 Прогнать `make trommel-image` и `make trommel-image-verify`; проверка — образ содержит
      анализатор закреплённой версии, запись в проверяемый проект отклонена
- [x] 7.3 Выполнить прогон контракта `full` на дереве Trommel и обновить `COVERAGE.md` целью
      `make trommel-coverage`; проверка — расхождение с прежней сводкой сводится к идентификации
      условий прогона, состояния правил `CLAR`, `CQRS`, `DDD` остались `--`
- [x] 7.4 Прогнать `sh tools/make-lint/check.sh` и `openspec validate add-code-facts-source
      --strict`; проверка — обе команды завершаются кодом `0`
- [x] 7.5 Прогнать подготовку на стороннем проекте, смонтированном пользователем, — на языке,
      отличном от Go; проверка — срез собран либо отказ назван причиной, а файлы, не разобранные
      анализатором, перечислены в границе среза, а не пропущены молча.
      Выполнено на `/app/projects/auditch-search` (PHP) вместе с задачей 8.8

## 8. Все уровни и карта проекта

- [x] 8.1 Завести карту проекта в `internal/scope`: сканируемые и исключённые пути, ответ
      на вопрос о принадлежности пути области; проверка — тесты на пустой карте, карте
      с одним путём и исключением внутри сканируемого пути
- [x] 8.2 Провести карту через прогон: ключ `--scan` командной строки, поле прогона, передача
      в фазу подготовки; проверка — тест: факт вне карты в срез не попадает
- [x] 8.3 Передавать сканируемые пути анализатору аргументом команды и приводить пути ответа
      к корню проекта; проверка — тест на подставном анализаторе с двумя сканируемыми путями:
      пути фактов даны от корня проекта, обращений — по одному на путь
- [x] 8.4 Описать модель фактов уровней `L3`..`L5` и разобрать вывод `reaching-defs`,
      `available`, `dead-stores`, `slice`; проверка — тесты на образцах настоящего вывода
      выпуска `0.4.0`, снятых с PHP-проекта
- [x] 8.5 Собирать уровни функций обходом функций уровня `L1` в пределах карты; отказывать
      при объявлении уровня функций без уровня структуры; проверка — тесты на оба случая
- [x] 8.6 Называть карту прогона в вердикте во всех трёх формах; проверка — тест: сканируемые
      и исключённые пути видны, срез части проекта не читается как срез проекта целиком
- [x] 8.7 Дополнить `docs/tldr-code.md` точечными командами, формами их вывода и перечнем
      неснимаемого (`taint`, `chop`) с причиной; проверка — прогон `docs`-контракта без `fail`
- [x] 8.8 Снять все уровни с `/app/projects/auditch-search` и записать замер времени и объёма;
      проверка — срез собран, непросмотренное названо, замер приведён в `design.md`
- [x] 8.9 Завести цель `make trommel-facts`: съём образом Trommel по пути на хосте, переменные
      `PROJECT`, `SCAN`, `LEVELS`, `FACTS_FORMAT`, `FACTS_DIR`, отказ на пустом каталоге,
      срез в `facts/` вне коммита; проверка — `sh tools/make-lint/check.sh` кодом `0`, цель
      видна в `make help`, раздел о съёме добавлен в `docs/tldr-code.md` и проходит
      `docs`-контракт. Цель вызывается пользователем на хосте: раннер передаёт только имя
      цели и переменные не пропускает, поэтому в его белый список она не добавлялась
