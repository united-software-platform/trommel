## 1. Раннер и цели проекта

- [ ] 1.1 Заменить поимённый белый список раннера на опознание целей проекта по признаку; проверка — новая цель `trommel-*` вызывается без правки `runner.py` и без перезапуска раннера
- [ ] 1.2 Согласовать с набором механизм расширения `Makefile` проектом — подключение файла целей проекта, если он существует; проверка — решение записано, и после обновления набора цели проекта остаются на месте
- [ ] 1.3 Перенести цели `trommel-*` из `Makefile` поставки в файл проекта; проверка — `make trommel-test` работает, `Makefile` совпадает с поставочным по `sha256`

## 2. Расхождение с поставкой

- [ ] 2.1 Реализовать сверку рабочего дерева с `.sdd-kit-manifest.json`; проверка — команда перечисляет изменённые файлы поставки и отличает их от отсутствующих
- [ ] 2.2 Зафиксировать текущие расхождения и решить по каждому: вернуть поставочный вид либо признать правку проекта осознанной; проверка — перечень расхождений пуст либо каждое названо с обоснованием

## 3. Версия и сборка

- [ ] 3.1 Определить источник версии сборки — файл версии репозитория либо тег; проверка — решение записано в `design.md` изменения, источник существует
- [ ] 3.2 Подставлять версию при сборке через параметры компоновщика; проверка — `trommel --version` печатает версию из источника, а не `0.0.0-dev`
- [ ] 3.3 Описать порядок получения бинарника в репозитории; проверка — по описанию сборка выполняется на чистой машине с Docker без обращения к переписке

## 4. Документация

- [ ] 4.1 Привести `README.md` в соответствие с работающим Trommel: контракт намерения, команда прогона, формы вывода, сводка покрытия; проверка — прогон по документам репозитория остаётся зелёным, а описание совпадает с поведением
- [ ] 4.2 Описать в `README.md` границу ответственности: Trommel выдаёт вердикт и не блокирует, блокирование выполняет вызывающая сторона; проверка — формулировка не обещает гарантии, которой продукт не даёт

## Версионирование

| Версия | Дата | Задача | Агент | Модель | Описание изменений |
|--------|------|--------|-------|--------|--------------------|
| 1.0.0 | 2026-09-22 | OpenSpec change `close-setup-gaps` | Claude Code | Claude Opus 5 | Начальное создание: раннер и цели проекта, расхождение с поставкой, версия и сборка, документация |
