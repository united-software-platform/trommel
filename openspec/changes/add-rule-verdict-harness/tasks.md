## 1. Каркас и поставка

- [x] 1.1 Завести модуль Go и точку входа командной строки; проверка — `trommel --version` печатает версию и завершается кодом `0`
- [x] 1.2 Настроить прогон тестов и статических проверок Go; проверка — команда прогона завершается успешно на пустом наборе тестов
- [x] 1.3 Собрать образ контейнера с непривилегированным пользователем по [DOCK-012](../../../rules/docker.md#пользователь-контейнера-приложения-dock-012-dock-013); проверка — внутри контейнера процесс исполняется не от `root`, рабочий каталог принадлежит пользователю приложения
- [x] 1.4 Реализовать монтирование проверяемого проекта только на чтение; проверка — тест подтверждает, что содержимое смонтированного каталога после прогона не изменено

## 2. Каталог правил

- [x] 2.1 Реализовать разбор таблиц правил документов нормы; проверка — на `rules/` каталог содержит 124 кода, число совпадает с подсчётом по таблицам
- [x] 2.2 Реализовать отнесение правила к группе по префиксу кода; проверка — запрос группы `DOC` возвращает 23 правила
- [x] 2.3 Реализовать реестр реализаций и признак наличия реализации у правила; проверка — правило без реализации присутствует в каталоге с признаком отсутствия
- [x] 2.4 Реализовать обнаружение висячей реализации; проверка — реализация, ссылающаяся на отсутствующий в каталоге код, приводит к ненулевому коду возврата и сообщению с этим кодом
- [x] 2.5 Реализовать отпечаток содержимого каталога; проверка — тест на двух состояниях документа нормы даёт разные отпечатки
- [x] 2.6 Реализовать отказ при неразобранной таблице правил; проверка — на документе с испорченной таблицей прогон не выполняется, код возврата `2`, в сообщении назван документ

## 3. Контракт намерения

- [x] 3.1 Определить формат файла контракта и его версию, реализовать разбор; проверка — тест разбирает образец контракта и извлекает состав и параметры
- [x] 3.2 Реализовать отказ при неизвестном поле и неподдерживаемой версии контракта; проверка — оба случая дают код `2` с указанием поля либо объявленной и поддерживаемой версий
- [x] 3.3 Реализовать проверку существования каждого упомянутого кода правила в каталоге; проверка — контракт с опечаткой в коде даёт код `2`, ни одно правило не исполняется
- [x] 3.4 Реализовать проверку заданности обязательных параметров правил состава; проверка — незаданный обязательный параметр даёт код `2` с указанием правила и имени параметра
- [x] 3.5 Реализовать включение в состав правила, не упомянутого контрактом; проверка — правило, добавленное в каталог и отсутствующее в контракте, появляется в выводе прогона
- [x] 3.6 Реализовать строгий режим исчерпывающего состава; проверка — в строгом режиме неучтённое правило каталога даёт код `2` с перечнем неучтённых правил
- [x] 3.7 Реализовать исключение правила с обязательным обоснованием; проверка — исключение без обоснования даёт код `2`, исключение с обоснованием попадает в отдельный блок вывода
- [x] 3.8 Реализовать объявление нескольких контрактов и выбор по имени; проверка — вызов с необъявленным именем даёт код `2` и печатает перечень объявленных контрактов
- [x] 3.9 Реализовать вычисление требуемых предметов проверки из состава правил; проверка — правило, которому не передан требуемый предмет, приводит к коду `2`, а не к статусу `ok`

## 4. Вердикт и вывод

- [x] 4.1 Реализовать пять состояний правила — `ok`, `fail`, `err`, `skip`, `--`; проверка — тесты покрывают каждое состояние и подтверждают их различимость в выводе
- [x] 4.2 Реализовать инвариант полноты для `ok`; проверка — при неразобранном содержимом правило получает `err`, а вывод называет это содержимое
- [x] 4.3 Реализовать вывод всего состава прогона с числом правил состава и числом проверенных; проверка — при частичном покрытии вывод содержит все правила состава, включая `--` и `skip`
- [x] 4.4 Реализовать структурированную форму вердикта наряду с читаемой человеком; проверка — обе формы содержат одинаковый набор состояний правил
- [x] 4.5 Реализовать коды возврата `0` / `1` / `2`; проверка — три исхода различимы, несостоявшийся прогон не даёт кода `0`
- [x] 4.6 Реализовать идентификацию условий прогона в выводе — версия Trommel, имя контракта, отпечаток каталога; проверка — сохранённый вердикт содержит все три значения
- [x] 4.7 Подтвердить воспроизводимость вердикта; проверка — повторный прогон на неизменных входах даёт совпадающие состояния правил

## 5. Правила группы DOC

- [x] 5.1 Довести до замкнутой формы формулировки правил, вводимых в зелёную зону, — в первую очередь `DOC-005` (исчерпывающий ли перечень HTML-конструкций) и `DOC-004` (граница множества нестандартных расширений); проверка — правка внесена в `rules/documentation-rules.md`, из формулировки однозначно следует множество нарушений
- [x] 5.2 Реализовать механику наборов образцов «обязано сработать» и «обязано смолчать»; проверка — правило без обоих наборов не может получить состояние `ok`
- [x] 5.3 Реализовать правила построчного разбора `DOC-005`, `DOC-006`, `DOC-007`, `DOC-008`; проверка — наборы образцов каждого правила проходят
- [x] 5.4 Реализовать `DOC-016` — язык у каждого блока кода; проверка — наборы образцов проходят, содержимое блоков кода не принимается за нарушение других правил
- [x] 5.5 Реализовать правила структуры документа `DOC-012` и `DOC-014`; проверка — наборы образцов проходят, включая документ с пропуском уровня заголовка
- [x] 5.6 Реализовать `DOC-015` с исключением для `README.md`; проверка — внешняя ссылка в `README.md` нарушением не считается, та же ссылка в прочем документе считается
- [x] 5.7 Реализовать `DOC-022` и `DOC-024` — наличие таблицы версий и возрастающий порядок строк; проверка — исключение для `README.md` соблюдается, нарушение порядка строк обнаруживается

## 6. Самопроверка репозитория

- [x] 6.1 Составить контракт намерения для этого репозитория с составом из группы `DOC`; проверка — `openspec`-артефакты и документы `rules/` попадают в состав, контракт проходит валидацию
- [x] 6.2 Выполнить прогон по собственным документам и зафиксировать вердикт; проверка — вердикт получен, число правил состава и число проверенных напечатаны
- [x] 6.3 Устранить обнаруженные нарушения в документах репозитория либо зафиксировать каждое оставшееся с обоснованием; проверка — повторный прогон не содержит необъяснённых `fail`

> **Примечание:** задача о шаге прогона в пайплайне вынесена в отдельное изменение
> `add-verdict-pipeline`: в репозитории нет ни пайплайна, ни каталога `.github/workflows/`,
> и её выполнение означает заведение пайплайна с нуля, а не правку существующего.

## Версионирование

| Версия | Дата | Задача | Агент | Модель | Описание изменений |
|--------|------|--------|-------|--------|--------------------|
| 1.0.0 | 2026-09-21 | OpenSpec change `add-rule-verdict-harness` | Claude Code | Claude Opus 5 | Начальное создание: каркас и поставка контейнером, каталог правил, контракт намерения, вердикт и вывод, правила группы `DOC`, самопроверка репозитория |
