# Секреты и локальное состояние окружения агента: создаются целью make init
# и в репозиторий не попадают.
.env
.ssh/
.claude-accounts/
.idea

# Артефакты сборки Trommel: собираются целью trommel-build и в репозиторий не попадают.
bin/
